version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18.x
    commands:
      - corepack enable
      - corepack prepare pnpm@latest-8 --activate
      - pnpm config set store-dir .pnpm-store
  build:
    commands:
      - sh deploy_frontend.sh "${ENV}"
